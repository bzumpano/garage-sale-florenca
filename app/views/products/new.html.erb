<% content_for :title, t('products.index.new') %>

<h1 class="text-2xl font-bold text-gray-800 mb-6"><%= t('products.index.new') %></h1>

<%= render "form", product: @product %>

<div class="mt-6">
  <%= link_to t('common.back'), products_path, class: 'text-blue-600 hover:underline' %>
</div>
